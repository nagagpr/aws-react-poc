{
  "name": "aws-react-deployment-poc",
  "private": true,
  "version": "1.0.0",
  "description": "Simple React + Vite dashboard used to compare AWS Amplify Hosting with Docker + Amazon ECR + AWS App Runner deployment.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^4.5.14"
  },
  "engines": {
    "node": ">=16.13.0"
  }
}
